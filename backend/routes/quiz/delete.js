const { Quiz } = require("../../models/quiz");

function registerDeleteRoutes(router) {
  // e.g. router.delete("/quizzes/:quizId", ...);
  // TODO when needed
}

module.exports = registerDeleteRoutes;
