/**
 * Fallback constants only — must match backend `constants/quizContribution.js`.
 * Contribution counts come from GET /api/quiz/:courseId/contribution-status (see quizApi).
 */
export const MIN_HUMAN_QUESTIONS_FOR_QUIZ = 5;
export const USER_CONTRIBUTION_THRESHOLD = 3;
